{
  "tools": [
    {
      "name": "list_agent_terminals",
      "description": "列出所有正在运行 agent CLI（如 codex、aider 等）的 Terminal 窗口信息",
      "inputSchema": {
        "type": "object",
        "properties": {
          "agentNames": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "要查找的 agent 名称列表，默认: [\"codex\", \"aider\"]"
          }
        }
      }
    },
    {
      "name": "send_to_agent",
      "description": "将消息发送到指定 agent 的 Terminal 输入框",
      "inputSchema": {
        "type": "object",
        "properties": {
          "agentName": {
            "type": "string",
            "description": "目标 agent 的名称（如 codex、gemini、qwen、iflow 等）"
          },
          "message": {
            "type": "string",
            "description": "要发送到 agent 输入框的消息内容"
          },
          "windowId": {
            "type": "number",
            "description": "可选：指定 Terminal 窗口 ID。如果不提供，将自动查找运行该 agent 的窗口"
          }
        },
        "required": ["agentName", "message"]
      }
    }
  ]
}
